# ── Model, training, inference ───────────────────────────────────────────────
# NOTE: on an NVIDIA Blackwell GPU (e.g. RTX 50-series) install torch FIRST from
# the CUDA 12.8 wheel index, then the rest of this file:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch
torch>=2.2
tokenizers>=0.15
numpy>=1.26
tqdm>=4.66
matplotlib>=3.8

# ── Data preparation (notebook 01) ───────────────────────────────────────────
datasets>=2.16      # load SQuAD v1.1
pandas>=2.0

# ── Baselines (notebook 02) ──────────────────────────────────────────────────
scikit-learn>=1.3   # TF-IDF
rank-bm25>=0.2.2    # BM25

# ── Running the notebooks ────────────────────────────────────────────────────
ipykernel>=6.25

# ── Optional ─────────────────────────────────────────────────────────────────
# faiss-cpu>=1.7     # ANN index in encode_and_eval.py (brute-force numpy works without it)
# wandb>=0.16        # live training curves via the --wandb flag
